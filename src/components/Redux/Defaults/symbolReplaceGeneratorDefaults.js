export const options = {
  useWrappers: true,
};

const symbolReplaceGeneratorDefault = {
  generatorName: 'symbolreplace',
  profile: '',
  options,
};

export default symbolReplaceGeneratorDefault;
