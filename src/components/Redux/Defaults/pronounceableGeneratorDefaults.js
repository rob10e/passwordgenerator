export const options = {
  length: 20,
  samples: 10,
};

const pronounceableGeneratorDefault = {
  generatorName: 'pronounceable',
  profile: '',
  options,
};

export default pronounceableGeneratorDefault;
